
<% if flash[:notice] %>
  <p><%= flash[:notice] %></p>
<% end %>
<div class="container">
  <div class="row">

    <div class="col-xs-3" >
      <table class="table table-responsive">
        <%= render 'books/list', newbook: @newbook, user: @user %>
      </table>
    </div>

    <div class="col-xs-9">
      <h2>Books</h2>
      <table class="table table-responsive">
        <thead>
          <tr>
            <th></th><th>Title</th><th>Opinion</th>
          </tr>
        </thead>

        <tbody>
          <% @user.books.each do |book| %>
          <tr>
            <td><%= link_to(user_path(@user.id)) do %>
	          <%= attachment_image_tag @user, :profile_image, :fill, 50, 50, format:'jpeg', fallback: "no_image.jpeg" , size:"100×100" %>
	            <% end %>
            </td>
            <td><%= link_to book.title, book_path(book.id) %></td>
            <td><%= book.body %></td>
          </tr>
          <% end %>
        </tbody>
      </table>
    </div>
  </div>
</div>